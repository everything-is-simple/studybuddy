__pycache__/
*.py[cod]
.pytest_cache/
*.sqlite3
*.db
*.db-wal
*.db-shm
.venv/
node_modules/
test-results/
playwright-report/
blob-report/
coverage/

# local test/runtime data and generated evidence
backend/.pytest_cache/
backend/test-results/
artifacts/
runs/

# regenerable machine-readable scan artifact (human .md report is tracked)
docs/frontend-inventory-scan.json

# pi API key material and local agent state (personal, never commit)
.pi/
.backup/
backup/

# local machine recovery/configuration artifacts (never commit)
settings.json
omp-windows-*.exe
tmp_sb/

# local runtime secrets and environment files
.env
.env.*
!.env.example

# local first-run helper overrides
first-run-studybuddy.local.ps1

# local agent/session state (never commit)
.workbuddy/

# local runtime data root (databases, uploaded originals, generated artifacts)
data/

# local runtime and generated output accidentally created under the repository root
/studybuddy-data/
/results/

# E2E 检测运行产物与本机运行日志（不提交）
server.log
server_err.log
server_out.log
.codebuddy/

# 已被运行时复检推翻的静态误报报告，保留在本地不入库
E2E_TEST_REPORT.md
test_e2e_report.md
