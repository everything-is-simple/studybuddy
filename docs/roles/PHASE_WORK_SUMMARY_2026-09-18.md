# StudyBuddy 阶段性工作总结报告

**报告日期**：2026-09-18（周五）
**编制依据**：`docs/[需求+所有角色看]STATUS.md`、`docs/[需求看]TODO.md`、`docs/INDEX.md`、工作记忆、`h:\studybuddy-data` 运行库实测、`git` 提交记录（`2026-09-16`～`2026-09-18` 共 6 次提交）。
**适用范围**：本地单机 v1（local single-process / single-instance / SQLite / local-disk），不含多用户、云同步、桌面安装包与框架迁移。

---

## 一、今日（2026-09-18）已完成工作事项

| # | 事项 | 关键产出 | 来源 |
|---|------|----------|------|
| 1 | 全站按钮操作引导上线 + 用户端到端检测 | 21 页渲染后 DOM 实测 119/119（100%）title 覆盖（legacy 页 43/43）；新增 107 用例全页面 E2E 规格，全部通过（exit 0） | 提交 `849b50e` |
| 2 | E2E 检测发现的 P1 问题修复 | 见第三节 P1-001/002/003 | 提交 `f9252ad` |
| 3 | 导入 6 本真实教材并完成 AI 索引 | 五年级上下（语文/数学）、四年级上下（语文）共 352,908 字 / 807 段，全部 `ready` | 提交 `f9252ad` |
| 4 | P2 用户体验改进 5 项 | 按钮 disabled 无原因提示、报告快速选择、空库新手引导、教材导入引导、桌面导航平铺 | 提交 `f9252ad` |
| 5 | 真实 Embedding Provider 验证完成 | 火山引擎 `doubao-embedding-vision`（2048 维，batch≤10）替换 fake embedding；6 本教材 529 个分块生成真实向量并写入库 | 提交 `2a9e7d2` + `54f3492` |
| 6 | 修复 embedding 索引两处缺陷 | `_legacy_part_15.py` SQL 占位符（15 值 14 列→14 值 14 列）、批次大小硬编码 32→`provider.max_batch_size` | 提交 `2a9e7d2` |
[REDACTED_STUDY_CONTENT]

> 注：后台监控进程（`monitor-studybuddy.ps1`，守护/自动重启/纠偏日志）已于 2026-09-17 上线，状态稳定（pid 19144 后端 / 20752 监控，liveness ok）。

---

## 二、已发现的具体问题及影响说明

| 等级 | 问题 | 影响说明 | 状态 |
|------|------|----------|------|
| P1-001 | 中文长句检索失败（如"什么是光合作用"） | FTS 无中文分词，整句词法检索必空，用户自然问句命中率极低 | ✅ 已通过 bigram 降级检索修复 |
| P1-002 | `retrieval_empty` 返回 HTTP 409（语义错误） | 409 表示"状态冲突"，但此处是"请求有效但当前无法满足"，语义误导前端 | ✅ 已改为 422，`retrieval_not_ready` 保留 409 |
| P1-003 | AI 环境变量易配错（如 `STUDYBUDDY_AI_PROVIDER_BASE_URL` 拼写） | 拼错后静默失效，用户无法定位 | ✅ 已加启动告警 + 设置页可点击入口 |
| 缺陷 | `_legacy_part_15.py` 两处缺陷 | ① SQL 占位符错位导致 INSERT 失败；② 批次硬编码 32 超过火山 API 上限 10，向量生成必然失败 | ✅ 已修复 |
| 环境 | 本机外网 TLS 被阻断 | `api.deepseek.com`/`ark.cn-beijing.volces.com`/`smtp.qq.com`/`smtp.163.com` 的 TLS 握手均 `SSLEOFError`（TCP 可达、TLS 被断）。DeepSeek、部分火山接口、QQ/163 邮箱投递**当前无法验收**；Agnes 与 Mistral 可达 | ⚠️ 环境事实，非代码缺陷，待网络恢复后补验 |
| 测试 | 622/632 后端测试通过，10 个失败 | 失败项均为**文档治理测试**，非功能缺陷；真实功能链路不受影响 | 🟡 待更新治理测试 |
| P3 台账 | live 数据库混有约 24 条测试残留材料 | 干扰真实库整洁度与运维判断 | 🟡 已知，待清理 |
| P2 台账 | `stored_path` 存 Windows 绝对路径 | 数据根不可迁移至其他机器/目录 | 🟡 已知架构限制 |
| P2 台账 | FTS 无中文分词 | 整句词法检索必空，依赖 hybrid 主路径 + bigram 降级 | 🟡 已用降级策略规避 |

---

## 三、已确认完成并验证通过的功能/模块

**核心链路（自动化 + 真实全链验证）**
- ✅ 材料导入/解析（PDF/TXT/DOCX/MD/JSON）→ 分块 → BM25 + 向量 + RRF 混合检索 → 问答带引用回溯 span
- ✅ 6 本真实教材导入完成，529 个分块生成**真实 2048 维火山向量**并入库，检索质量已验证
- ✅ 真实全链跑通（P2-USE-4）：真实 PDF/DOCX + 板书 PNG 的 PaddleOCR（置信度 0.941–0.999）+ MP3 的 whisper.cpp 转写 → 卡片/练习草稿 → 计划/节奏/进度 → 周报导出
- ✅ 后端 622/632 测试通过；前端 21 页控件矩阵 549 项记录、479 通过、70 条件阻塞、0 失败；107 用例 E2E 全过

**工程与运维**
- ✅ 迁移系统（v1→v15 单向迁移、回滚、PRAGMA user_version 强一致）、备份/恢复/verify（35 测试通过）
- ✅ 可观测性：结构化安全日志、request→operation→task 关联、liveness/readiness/degraded 三态、metrics
- ✅ 单进程 task runner（lease/retry/cancel/stale recovery）、后台监控进程
- ✅ 能力自动探测 + 仪表盘 + 配置可持久化（P2-USE 系列）
- ✅ Provider/Email 连接测试（28 测试）、secret 泄漏扫描（0 findings）、配置先测后存

**组件门禁（Composer→Integration→Formal C0-C6 scoped closeout）**
- ✅ B1 真实 ASR（whisper.cpp）、B2 真实 OCR（PaddleOCR + RapidOCR 回退）、B3 报告、B4 投递（QQ SMTP / 飞书 webhook，默认 off）

---

## 四、系统当前整体进度

| 维度 | 状态 | 完成度 |
|------|------|--------|
| Phase 1–10（local v1 上线收口，Gate A–J） | ✅ completed | 100% |
| 21 个正式页面 A/B 审查 + 全页面控件矩阵 | ✅ completed | 100% |
| 核心 AI 闭环（导入→检索→问答→引用→卡片→计划→报告） | ✅ 真实全链跑通 | 100% |
| 真实 Embedding / 真实 Provider（特定） | ✅ Mistral / Agnes / 火山 doubao / glm 已验 | 已验证子集 |
| 组件能力（B1–B4） | ✅ scoped closeout | 100%（声明范围） |
| 文档治理测试 | 🟡 10 项待更新 | ~98% |
| 真实自用观察期（P1-7） | ⬜ 未真正执行 | 0% |
| 验证广度（其它 Provider / OCR 准确率 / 跨浏览器 / 屏幕阅读器 / 真实外发） | ⚠️ not_verified | — |
|  deferred：TTS / 桌面化 / 框架迁移 | ⬜ 暂缓 | 0% |

**整体完成度评估**：按"本地 v1 既定范围内可用 + 自动化验证"口径，**功能性完成度约 88%**，自动化测试覆盖完成度 **>95%**。剩余约 12% 主要为：① 真实 Provider 验证广度扩展；② P1-7 真实自用观察闭环；③ 文档治理测试补齐；④ 已知台账项（测试残留材料清理、路径可移植性）收尾。**注意**：`not_verified` 项（跨浏览器、系统级屏幕阅读器、真实断电/ACL/磁盘满、通用多 Provider 准确率）不属于"已实现但未测"，按项目诚实标注规则仍不得宣称 real-pass。

**剩余工作量评估**：
- 高价值低风险：P1-7 真实自用观察 + 治理测试补齐（预计 0.5–1 周）
- 中价值：扩展 Provider 验证（DeepSeek/Agnes 其余 profile、ARK 等，受网络限制待解）
- 暂缓（非阻塞）：TTS 立项、桌面化、前端框架迁移（触发条件未满足，不启动）

---

## 五、下一步需执行任务清单（按优先级）

| 优先级 | 任务 | 说明 | 依赖/条件 |
|--------|------|------|-----------|
| P0 | 清理 live 库约 24 条测试残留材料 | 数据整洁、避免运维误判 | 无，可直接执行 |
| P0 | 补齐 10 个文档治理测试失败 | 恢复 632/632 全绿基线 | 无 |
| P1 | 启动并坚持 P1-7 真实自用观察期（7 天） | 用 6 本真实教材做导入/搜索/问答/计划/练习/报告全场景，回流卡壳点 | 使用者本人执行 |
| P1 | 积累向量检索质量基线（五年级学生问答场景） | STATUS 已建议：对 6 本教材跑更多真实问答，建立质量基线 | 火山 embedding 已可用 |
| P1 | 复查本机外网 TLS 阻断问题 | 恢复后补验 DeepSeek / 火山 / QQ 邮箱投递 | 网络/防火墙环境修复 |
| P2 | 扩展真实 Provider 验证（其它 model/profile） | 不阻塞 v1，但提升能力声明上限 | 网络恢复 |
| P2 | 监控 `config.py` 启动警告日志 | 确认用户是否仍遇变量名配置错误 | 运行观察 |
| P3 | TTS / 桌面化 / 框架迁移评估 | 仅当触发条件满足后立项 | 现有 Web 功能全集成验证后 |

---

## 六、阶段性小结

**关键成果**
1. 本地 v1 核心闭环已真实跑通：从真实教材导入、真实 OCR/ASR、真实向量检索到带引用问答与练习报告，均经自动化 + 真实链路双重验证。
2. 今日突破点：6 本真实教材完成真实向量索引（火山 2048 维）、中文长句检索降级、检索语义错误修正、AI 配置易错点引导，前端 21 页 100% 按钮引导覆盖 + 107 用例 E2E 全过。
3. 工程治理扎实：迁移/备份/可观测性/组件 C0-C6 门禁齐备，能力自动探测与配置持久化解决"开箱即锁死/手抄 env"顽疾。

**主要风险**
1. **环境网络阻断**（本机 TLS 被断）使部分真实 Provider/邮箱投递无法验收，存在能力声明盲区。
2. **诚实标注鸿沟**：大量 `not_verified` 项（跨浏览器、屏幕阅读器、真实断电/ACL）仍不可宣称 real-pass，对外沟通需谨慎。
3. **live 数据残留与路径不可移植**（P2/P3 台账）属已知技术债，需定期清理与评估。
4. **真实自用观察期缺位**：功能可用 ≠ 用户真正顺畅使用，P1-7 未执行是上线前最大未知数。

**后续建议**
- 立即执行 P0 两项（清理残留 + 治理测试补齐），恢复全绿基线。
- 优先推进 P1-7 真实自用观察，将卡壳点直接回流为 bug 修复，而非新增审计切片（符合"可用优先"原则）。
- 网络恢复后集中补验受阻断的真实 Provider，扩大验证广度。
- 暂缓 TTS / 桌面化 / 框架迁移，直到现有 Web 功能全部完成 `/app` 集成与正常/失败用户路径验证。

---

*生成说明：本报告基于仓库权威状态文档与运行库实测整理；`not_verified` 项严格按项目诚实标注规则处理，不夸大已完成范围。*
