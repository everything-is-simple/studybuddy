# 9B-1：正式领域契约与状态机

> 先使用 `00_COMMON_CONTEXT.md`，再使用 9B-0 审计结果。本任务只冻结契约，不实现表、API 或 UI。

```text
执行 Phase 9B-1：冻结 S1/S2 的正式领域契约和状态机。

先读取 9B-0 审计初稿和实际源码。必须明确并写入正式契约：
- S1 节奏对象与 Phase 9A study plan/item 的关系；周期（日/周/自定义）的选择；timezone、日期边界、工作量单位、上限和非法输入；节奏分配、移动、跳过、完成、重开和 summary 重算规则；
- S2 note、note block、knowledge module、source link、citation 的关系；笔记是否允许无 source；模块/笔记是否可跨 material/revision；citation 绑定 note 还是 block；用户编辑、确认、归档和 stale 状态保护；
- AI draft → user edit → confirm/reject/archive 状态转移；fake-provider operation、idempotency、retry 和失败残留；不得静默覆盖用户笔记、已确认模块或完成状态；
- material delete/restore/purge、new extraction/revision、chunk re-index 后的 valid/stale/source_deleted/source_unavailable 映射；active 对象遇到 unavailable source 时是允许并 warning 还是禁止；
- project scope、未来 user boundary、隐私、导出对象和 backup/restore non-repair。

给出实体关系图或表格、字段/枚举草案、状态转移表、不变量、稳定错误码、API resource 草案和 deferred decisions。契约必须能直接驱动 migration、repository 和 browser tests。不要加入 S3/S4/S5、S6/S7、scheduler、worker 或真实 Provider acceptance。

验收：领域术语无歧义；S1/S2 与 9A/Phase 8 边界清晰；所有跨行规则标明 domain transaction enforcement；状态使用 `planned/contract-frozen`，不能写 implemented 或 completed。
```