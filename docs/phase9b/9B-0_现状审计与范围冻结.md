# 9B-0：现状审计与范围冻结

> 先使用 `00_COMMON_CONTEXT.md` 和 `00_MASTER_PLAN_PROMPT.md` 作为上下文。本任务只审计和规划，不实现业务代码。

```text
执行 Phase 9B-0：只做现状审计、S1/S2 范围冻结和风险记录，不新增 migration，不实现 repository、API 或 UI。

审计并记录：当前实际 schema version 和 migration runner；Phase 9A 的 goal/module/plan/item/dependency/progress/source-link 表、事务和 API；materials/revisions/chunks/retrieval/citation 的 source lifecycle；Phase 8 Cards/Exercises 的 draft/edit/citation contract；现有 ai_operations/provider registry/fake provider；main.py 前端 workspace、导出路由、统一错误和测试 fixture。

对照历史场景文档只提取需求线索，不复制历史 KnowledgeModule、StudyRhythm 或 S1/S2 代码。明确 S1 学习节奏和 S2 资料笔记的最小用户目标、正式系统要复用的能力、必须新增的能力，以及 9C/9D/Phase 10 non-goals。重点识别：note/module/block/citation/source-link 是否已有可复用对象；节奏如何附着 Phase 9A plan/item；哪些内容是用户状态，哪些是 AI draft；导出和 backup/restore 是否天然覆盖。

产出：`docs/phase9b/PHASE9B_DOMAIN_CONTRACT.md` 审计初稿，包含 glossary、现状证据表、候选对象关系、S1/S2 non-goals、风险、开放决策和建议的子任务边界。必要时只更新 `docs/TODO.md` 的 Phase 9B 规划引用，不能标记实现完成。

验收：每个重要结论有源码路径、函数或测试名称；git diff 只包含允许的文档；状态使用 `planned/audit-draft`；没有修改生产逻辑、migration、API 或 UI。
```