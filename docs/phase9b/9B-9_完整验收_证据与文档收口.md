# 9B-9：完整验收、证据与文档收口

> 先使用 `00_COMMON_CONTEXT.md`、9B contract、执行门禁和全部实现任务。本任务只做验收、证据和文档收口；不要在 closeout 中偷偷增加功能。

```text
执行 Phase 9B-9：完成 S1/S2 全量回归、脱敏 acceptance evidence 和文档状态收口。

运行实际存在且与当前源码匹配的：9B contract/migration/domain/S1/S2/API/source lifecycle/backup tests；完整 `backend/tests/`；`browser_phase9b.spec.js`；Phase 8、Phase 9A 相关 Chromium regression；frontend failure contract；必要的 restore acceptance。记录实际命令、环境、passed/skipped/failed、测试 worker/viewport、隔离 data root 和 artifact 位置。不要把历史文档中的测试数字当作当前结果。

新增 `docs/PHASE9B_ACCEPTANCE_EVIDENCE.md`，必须包含：范围和 non-goals；Gate A-I 结果；S1 用户路径；S2 用户路径；source lifecycle；backup/verify/restore/non-repair；API/UI 失败边界；隐私与安全检查；真实 Provider、人工审核、scheduler/worker、screen reader、极端内容、长时稳定性、磁盘满、断电、多进程等未验证边界。

同步唯一事实源：`docs/STATUS.md`、`docs/TODO.md`、`docs/PHASE_ROADMAP.md`、`docs/PROJECT_PROGRESS_REPORT.md`，必要时更新 `docs/ai-learning-architecture.md` 和 `docs/INDEX.md`。不得把 9B prompt、设计文档、fake-provider 局部测试或单个 browser pass 当成完整完成证据。

只有以下全部满足才允许写 completed：S1/S2 契约冻结；连续 migration/rollback；domain transactions；S2 note/module workflow；S1 rhythm workflow；API boundary；Chromium happy/failure/narrow/keyboard/reload；source lifecycle；backup/restore non-repair；完整 backend/相关 browser regression；脱敏 evidence 和文档同步。

准确状态措辞：
“Phase 9B 已在 deterministic fake-provider、单进程 SQLite、本地 Chromium 与 backup/restore 的明确范围内完成；不代表 Phase 9C/9D、真实 Provider generation、scheduler/worker、人工复核或全局 production real-pass。”

如果任何 gate 未通过，保留 `planned` 或局部 gate 状态，列出阻塞项，不得宣布 Phase 9B completed。
```