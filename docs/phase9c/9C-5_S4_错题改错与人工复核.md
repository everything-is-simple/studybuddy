# 9C-5 S4 错题改错与人工复核

## 执行记录

状态：`implemented/backend-pass`。

- 基于真实 append-only attempt/grading/review 事实完成 S4 ErrorFixer backend：`correct`/`incorrect`/`uncertain` short-answer review、用户显式 `user_marked`、mistake case detail/list、occurrence/feedback history、weak-point projection、archive 和 redo。
- 错题事实来源严格区分：MC/TF deterministic incorrect、short-answer human review incorrect、user explicit mark；pending/uncertain 不自动形成 mistake。AI/provider 建议不进入事实表。
- case 按 project/exercise/revision fingerprint 归并；重复 occurrence 幂等；fixed case 新 occurrence 进入 `reopened`；archive 为 terminal。redo 始终创建新的 practice session/attempt，旧 attempt、review、feedback 和 occurrence 保留。
- review 只允许 pending-review attempt 和本地 `local_user` reviewer；不修改原 attempt 的 grading fact，不把 review 伪装为 deterministic；普通返回不暴露 answer key 或 submitted answer。
- source soft-delete/purge 只把 occurrence/source snapshot 降级为 `source_deleted`/`source_unavailable`，保留 mistake、attempt 和 feedback 历史；rollback trigger 测试确认 review/feedback 失败不会留下半条事实。
- 新增 focused coverage：uncertain 与 user-marked 区分、review privacy/deterministic boundary、redo/history、append-only rollback、source lifecycle、case detail/weak-point/archive。
- 实际命令与结果：`C:/miniconda/py310/python.exe -m pytest backend/tests/test_phase9c_domain.py -q` 为 `13 passed`；相关 focused regression 为 `44 passed`；完整 `C:/miniconda/py310/python.exe -m pytest backend/tests/ -q` 为 `312 passed, 2 skipped`。两个 skip 是 opt-in real-provider smoke，不构成 9C 证据。
- 本任务未实现 S5、FastAPI routes、Chromium UI、9C lifecycle/backup/restore 专项 acceptance 或 Phase 9C closeout。


```text
执行 Phase 9C-5：实现 S4 ErrorFixer backend workflow，严格基于真实 attempt/grading/review 事实，不重写历史。

定义并实现错题进入条件：deterministic incorrect、pending_review 的人工决定、用户显式标记等必须区分；不得把 provider 建议或模糊答案自动当作错误事实。错题应有可审计 lifecycle（open/in_review/fixed/archived 等以 9C-1 为准）、安全原因/建议、citation/source status 和 weak-point 关联。重做永远创建新 attempt；修复/关闭操作不能删除或修改原 attempt、评分或人工 review。

实现短答人工复核：明确 reviewer boundary（当前单用户/本地范围）、reviewed/correct/incorrect/uncertain 语义、feedback 长度和隐私；reviewed 不能伪造 deterministic，submitted answer 和 answer key 不进入普通列表/DOM。source stale/deleted/purged 只降级 citation/warning，不伪造证据。AI 生成反馈/变题仅作为 draft/suggestion，失败保留安全 operation，不覆盖用户 feedback。

覆盖 focused tests：错题归并、重复 attempt、reopen/fixed、人工复核状态、uncertain、privacy、source lifecycle、rollback、backup-friendly persistence。只实现 backend domain/repository 及测试，不实现 S5/UI/API 总收口。状态为 implemented/backend-pass。
```