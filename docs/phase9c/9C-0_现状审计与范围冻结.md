# 9C-0 现状审计与范围冻结

```text
执行 Phase 9C-0：只审计和冻结范围，不实现 S3/S4/S5 业务代码。

使用 ../00_COMMON_CONTEXT.md。完整读取项目权威文档并审计实际 backend/app/migrations/runner.py、repository.py、main.py、backup.py、restore_acceptance.py，以及 test_phase8_*、test_phase9a_*、test_phase9b_* 和 browser_phase8/9a/9b tests。输出每个结论的源码路径、函数名或测试名。

必须盘点：当前 schema/version；exercise/card/attempt/review/ai_operation 实际字段和状态；Phase 8 grading/privacy；9A plan/module/progress；9B note/rhythm；citation/source lifecycle；现有导出、backup/restore、错误映射和前端结构。

冻结 S3 限时练习、S4 错题/人工复核、S5 冲刺的用户价值、依赖、风险和 non-goals。明确不把历史项目 S3/S4/S5、真实 Provider、自动排程、OCR/ASR、多用户或 Phase 10 worker 当现状能力。提出需要在 9C-1 决定的未决问题清单，包含时间、答案安全、重做、错题归并、人工复核和冲刺范围。

只允许修改 docs/phase9c/ 的审计文档（如需新增）；不改 backend/app、migration、tests、README/STATUS/TODO。验收：审计可复核、边界无重叠、阻塞项明确。状态只能写 planned/audit-draft。
```