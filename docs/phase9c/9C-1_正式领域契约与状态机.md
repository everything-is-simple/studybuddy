# 9C-1 正式领域契约与状态机

```text
执行 Phase 9C-1：在 9C-0 审计基础上冻结 S3/S4/S5 正式领域契约，不实现业务代码。

先验证当前源码，不能假设 Phase 8 schema。形成可驱动 migration/repository/tests 的正式文档，至少冻结：
- PracticeSession 的 owner、exercise snapshot、状态、server start/deadline/submit/timeout、timezone/date、刷新和重复提交；
- attempt 的 append-only 关系、重做新 attempt、MC/TF deterministic grading、short_answer pending_review、人工 review 状态和安全反馈；
- Mistake/ErrorFix 的事实来源、归并键、status、错因/建议/用户 override、重做结果和历史不可覆盖；
- WeakPoint 的派生/持久化策略、source/citation 关系、不可把 AI 猜测当事实；
- CramGoal/ExamSession/选题快照、S5 生命周期、S3/S4 结果引用和不自动修改 9A plan；
- AI draft/operation、citation revalidation、idempotency/retry、provider/raw data 隔离；
- SQLite constraint/domain transaction 分工、错误码、API/导出、source lifecycle、backup/restore non-repair。

明确 user-created 与 AI-generated、draft/confirmed/active/finished/expired/review_pending/reviewed/archived 等状态是否需要，避免把未经需求支持的状态直接加入 schema。冻结 S3/S4/S5 与 9D/Phase 10 的 non-goals。

只修改 docs/phase9c/ 契约文档。验收：每个状态转移、输入边界、隐私规则和异常行为无歧义，可写 focused tests。状态只能写 planned/contract-frozen。
```