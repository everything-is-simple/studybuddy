# 9C-10 完整验收、证据与文档收口

```text
执行 Phase 9C-10：只做 Phase 9C Gate A-J closeout、脱敏证据和文档同步，不偷偷补充新业务。

先确认 9C-0 至 9C-9 每个 gate 的实际证据和状态，运行项目规定的完整 backend：C:\\miniconda\\py310\\python.exe -m pytest backend/tests/ -q；运行相关 Phase 8/9A/9B regression、browser_phase9c.spec.js 和 UI failure contract。若有真实 Provider spec，默认 skip 或显式说明，不把 opt-in 结果扩大为通用 real-pass。

新增 docs/PHASE9C_ACCEPTANCE_EVIDENCE.md，记录范围、版本、migration、S3/S4/S5 用户路径、API/domain/browser/lifecycle/backup 证据、实际命令和结果、隐私检查、失败边界和未验证限制。同步 docs/STATUS.md、TODO.md、PHASE_ROADMAP.md、PROJECT_PROGRESS_REPORT.md、INDEX.md、ARCHITECTURE/ai-learning-architecture（仅在事实需要时）。不要保留与唯一状态源冲突的旧数字。

只有所有 Gate A-J 实际通过，才允许使用限定声明：Phase 9C 已在 deterministic fake-provider、单进程 SQLite、本地 Chromium 与 backup/restore 的明确范围内完成。必须同时写明不包含真实 Provider generation、自动 scheduler/worker、OCR/ASR、Phase 9D、全局 production real-pass，以及系统级 screen reader、极端内容、长时稳定性等未验证项。任何 focused/backend/browser/restore 局部通过只能写对应局部状态。

本任务允许修改 docs/ 和新增 evidence/test artifact（不得提交数据库、上传原件、secret、路径含私人数据或测试输出）；完成后报告每个 gate、命令、结果、准确完成措辞和遗留限制。
```