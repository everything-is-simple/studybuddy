# 9C-4 S3 限时练习工作流

## 执行记录

状态：`implemented/backend-pass`。

- 在 9C-3 shared domain 之上完成 S3 PracticeRunner backend 闭环：practice session create/read/list/start/submit/finish/expire/result，题目选择为 immutable snapshot，暂停/恢复未实现且没有隐式 action。
- session start 只由服务端写入 `started_at`/`deadline_at`；read/list/submit/finish 均以服务端 UTC 时间回收 deadline。deadline 到达后的 submit 稳定拒绝，finish 返回持久化 `expired` 的安全结果。
- submit 仅按 session item snapshot grading：MC/TF deterministic，short answer `pending_review`；不接受客户端 elapsed、score、correctness、deadline 或 finished time。session result 为只读汇总，含 scored/pending/unanswered/source warning 和 score ratio。
- 同一 item 的首次提交形成 append-only attempt；无 key 重复 item 返回原安全 replay。带 submission key 的同答案请求 replay，不同答案稳定返回 idempotency mismatch；不会覆盖旧 attempt。
- focused tests 覆盖 session 生命周期、MC/TF/short answer、finish/expired、result/list/reload、deadline、duplicate/idempotency、snapshot/privacy、rollback 和 source delete/purge read path。响应不含 answer key、submitted answer、source full text 或 stored path。
- 实际命令与结果：`C:/miniconda/py310/python.exe -m pytest backend/tests/test_phase9c_domain.py -q` 为 `8 passed`；相关 focused regression 为 `39 passed`；完整 `C:/miniconda/py310/python.exe -m pytest backend/tests/ -q` 为 `307 passed, 2 skipped`。两个 skip 是 opt-in real-provider smoke，不构成 9C 证据。
- 本任务不提供 S4/S5 独立 workflow、FastAPI routes、Chromium workspace、9C lifecycle/backup/restore 专项 acceptance 或 Phase 9C closeout。


```text
执行 Phase 9C-4：在共享 domain 层上完成 S3 PracticeRunner backend workflow，不实现 S4/S5 UI 或全 Phase API 收口。

冻结并实现显式 session create/start/submit/finish/expire/read。练习选择必须形成不可被后续普通编辑静默改变的 session item 快照；只允许符合契约的 ready exercise，复用 Phase 8 三种题型和确定性评分。服务端生成 started_at/deadline/status，客户端只能请求动作，不能提交 score、elapsed、correctness 或 finished_at。明确是否支持 pause/resume；若未冻结则拒绝，不隐式实现。

逐题提交生成新 append-only attempt；重复同一请求必须遵守契约的 idempotency，重做不能覆盖旧 attempt。超时、越权 exercise、过期 session、错误 payload、provider/source failure、refresh 和安全反馈都要有稳定错误。普通响应和日志不得暴露 answer key、内部答案、用户提交原文或 source path。

新增 focused backend tests，必要时可修改 repository/domain 和对应测试；不要扩展到 S4 mistake projection、S5 cram API、Chromium。验收：session 生命周期、deadline boundary、grading、attempt history/privacy、rollback、source lifecycle 读路径通过。状态为 implemented/backend-pass。
```