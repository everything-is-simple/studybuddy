# 9C-6 S5 期末冲刺工作流

## 执行记录

状态：`implemented/backend-pass`。

- 基于 S3 shared practice session 和 S4 mistake/weak-point domain 完成 S5 ExamCrammer backend：cram goal list/detail/transition、显式 cram session 创建、题目快照和 cram result summary。
- goal 生命周期为 `draft → active → completed/archived`；completed 必须存在 finished/expired cram session。目标日期/时区只作业务展示坐标，不启动 scheduler，也不作为 session deadline。
- cram session 使用 `practice_sessions.session_kind='cram'`，复用 S3 的 session/item/attempt/grading/expiry/result；不建立第二套 attempt、score 或 mistake 事实。选择必须显式提供、同 project、ready、distinct，且不超过 goal target count/50 项。
- result 只汇总 deterministic score、pending review、mistake count、weak-point 和 source warning；不返回 answer key、submitted answer、raw source text 或 stored path。
- goal/session 写入不修改 `study_progress_events`、plan/item 状态或 rhythm allocations；source stale/deleted/purged 仅保留历史并显示安全 warning。选择快照失败 rollback 测试通过。
- 实际命令与结果：`C:/miniconda/py310/python.exe -m pytest backend/tests/test_phase9c_domain.py -q` 为 `15 passed`；相关 focused regression 为 `46 passed`；完整 `C:/miniconda/py310/python.exe -m pytest backend/tests/ -q` 为 `314 passed, 2 skipped`。两个 skip 是 opt-in real-provider smoke，不构成 9C 证据。
- 本任务未实现 FastAPI routes、Chromium UI、9C lifecycle/backup/restore 专项 acceptance 或 Phase 9C closeout。


```text
执行 Phase 9C-6：实现 S5 ExamCrammer backend workflow，复用 S3 session、S4 feedback 和 Phase 9A module/plan，但不自动改写计划或启动排程。

先用源码验证已有对象，再按冻结契约实现 cram goal/exam session/item selection/result summary。用户必须显式选择范围、目标日期/时长和题目；题目选择形成快照，session 结束/超时/重做遵守 S3 语义。结果可引用 confirmed attempts、mistakes、weak-points 和安全 source status，但不得把建议当事实，不得自动创建 progress、rhythm allocation、提醒或新的学习任务。

覆盖：目标/session 生命周期、选题数量/时长上限、空集合/重复/跨 project、expired/duplicate submit、S3/S4 linkage、source unavailable/stale、privacy（answer key/submitted answer/raw source 不外泄）和事务 rollback。若需要 AI 生成冲刺题目/解析，只允许 deterministic fake-provider、draft、citation revalidation、safe operation 和显式确认；真实 Provider 不在范围。

修改 backend/app/domain/repository 及 focused tests；不实现 API/UI/restore closeout。状态为 implemented/backend-pass，并明确后续 9C-7 阻塞项。
```