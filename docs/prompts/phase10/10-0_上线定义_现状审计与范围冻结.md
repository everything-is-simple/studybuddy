# 10-0 上线定义、现状审计与范围冻结

```text
请在 H:\\studybuddy 执行 Phase 10-0。只做审计、上线定义和范围冻结，不修改生产代码，不新增 migration，不运行真实外部动作。

先读取 00_COMMON_CONTEXT.md 及其要求的全部权威文档，再审计实际源码、测试和当前 v12 数据/运维边界。建立“已实现/已测试/未验证/不支持”矩阵，至少覆盖：启动入口与配置、materials/source lifecycle、AI operations、Phase 8/9A–9D 用户路径、migration、backup/verify/restore、restore acceptance、observability、CLI、Chromium 测试、默认 provider 行为。

冻结本项目的上线定义：local single-process、single-instance、SQLite、local disk、本地浏览器。明确目标用户、安装/启动/停止/升级/备份/恢复/故障诊断流程、数据目录责任、Provider 可选性、发布版本与支持平台。将多用户、认证授权、云同步、协作、多 worker/shared data_root、真实 OCR/ASR、真实 SMTP/飞书、自动 scheduler、真实断电恢复列为 non-goals 或单独 gate，不能偷偷变成上线前置。

给出 Phase 10 任务边界、风险和 go/no-go：哪些必须在本地 v1 前完成，哪些可以 not_verified 后续验证；定义 P0/P1/P2 阻塞标准、上线回滚标准、保留/删除责任。不要因为已有 fake/loopback 或局部 real-pass 而扩大声明。

只允许修改 docs/prompts/phase10/、必要的 docs/TODO.md、docs/STATUS.md、docs/PHASE_ROADMAP.md、docs/PROJECT_PROGRESS_REPORT.md、docs/INDEX.md、README.md；若发现代码问题只记录，不顺手修复。产出审计结论、支持矩阵、范围决策、Gate A 验收项和下一任务阻塞条件。推荐提交：`docs: define local v1 launch scope and phase 10 gates`。准确状态：`planned/audit-draft`；该状态不代表上线或实现完成。
```
