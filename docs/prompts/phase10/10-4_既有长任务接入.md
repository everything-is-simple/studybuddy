# 10-4 既有长任务接入

```text
请在 H:\\studybuddy 执行 Phase 10-4。先读取共用上下文、10-1/10-3 契约和 runner 测试，盘点当前真正适合后台执行的操作。只接入经审计批准的最小集合，建议优先 explicit indexing/embedding；AI generation、OCR/ASR、report/delivery 必须分别确认副作用、用户确认和默认关闭边界。

为每个接入操作定义 handler 输入白名单、project scope、idempotency、状态/进度、可重试错误、不可重试错误、cancel 检查点、source revision/citation 绑定和最终产物原子写入。保持既有同步 API 的兼容或明确返回 task handle；不要静默改变现有 API 语义。后台执行不得将 draft 变 confirmed，不得覆盖用户编辑，不得把 stale/unavailable source 变 valid，不得触发真实外发。delivery 若接入只能走默认 off、allowlisted dry-run 且保持显式授权检查。

测试每种操作的 enqueue/read/retry/cancel/stale/restart/idempotency/rollback/source lifecycle/privacy；验证同一任务不会重复 provider 副作用或重复 artifact。真实 Provider/OCR/ASR/外发默认跳过；fake/loopback 只做可重复测试。运行相关 focused、完整 backend 和受影响 Chromium。

若某操作不适合后台化，明确保留同步并记录理由。验收 Gate E：批准操作接入且无既有 Phase 4–9 回归。推荐提交：`feat: connect approved long operations to task runner`。准确状态：`implemented/backend-pass`，真实外部配置仍按精确 gate 标记。
```
