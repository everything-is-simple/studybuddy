# 10-9 Release Candidate、上线演练、证据与文档收口

```text
请在 H:\\studybuddy 执行 Phase 10-9。先读取共用上下文、10-0 至 10-8 的全部产物和权威状态文档。此任务是最终验收和文档收口；若任一阻塞 Gate 未通过，停止并输出 no-go，不得为了“完成”修改证据或扩大声明。

在全新隔离 data root 中执行 release candidate 端到端演练：安全配置 → 安装/启动 → liveness/readiness → 导入材料 → 显式 indexing/必要后台任务 → Q&A/卡片/练习/学习计划/S1–S7 已批准路径 → task 状态/失败/retry/cancel/restart → 原文/报告导出 → backup → verify → restore 到新空目录 → restore acceptance/read → 重启 → migration upgrade（如有）→ operator diagnostics → 停止。真实 provider/OCR/ASR/外发只按显式 opt-in gate，不纳入默认 release gate；delivery 默认 off。

执行完整 backend suite、所有受影响 Chromium spec、migration/backup/restore/runner/observability focused tests 和发布 smoke。检查 git status，确保没有数据库、originals、secret、private path、Playwright report 或未脱敏 artifact。核对版本、schema_migrations、PRAGMA user_version、启动命令、恢复命令、已知限制和错误码。

生成正式脱敏 Phase 10 acceptance evidence：commit/version、环境、命令、结果、Gate A-J、跳过项、not_verified、失败和回滚记录。同步 docs/STATUS.md、TODO.md、PHASE_ROADMAP.md、PROJECT_PROGRESS_REPORT.md、README.md、INDEX.md，并保证只维护唯一事实源。若通过，使用准确措辞：
“Phase 10 已完成，StudyBuddy 已在明确的 local single-process/single-instance/SQLite/local-disk v1 支持范围内完成生产化和上线收口。”
同时必须附带：不代表多用户、认证授权、云同步、协作、多进程共享 data_root、真实断电恢复、所有真实 Provider/OCR/ASR/外发渠道或全局 production real-pass；未验证项目保持 not_verified。

若失败，只能写 release candidate no-go 和局部 gate 状态。推荐提交：`docs: close phase 10 release acceptance`。准确状态：全部通过为 `completed`（限定 local v1）；否则为 `release-candidate-no-go` 或对应局部状态。
```
