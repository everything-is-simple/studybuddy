# 10-8 容量、性能、长时稳定性与故障边界验收

```text
请在 H:\\studybuddy 执行 Phase 10-8。先读取共用上下文、10-0 的支持矩阵、I4 既有基线、测试治理和实际发布/runner/backup 入口。只做隔离环境中的可复现边界验收，不修改业务功能来掩盖失败。

定义时间盒和阈值，至少测量：首次启动/升级、单/批量材料导入、搜索/导出、revision/chunk/indexing/embedding、Q&A、Cards/Exercises、9A–9D 关键流程、task queue latency/progress/retry、backup/verify/restore、内存/数据库增长，以及有限长时 lifecycle smoke。测试 data root 必须在 H:\\studybuddy-test 或系统临时目录；artifact 脱敏，不保留正文、原件、路径、secret 或 provider raw response。

在安全可行时验证 Windows ACL/只读目录、配额/空间不足等边界；若环境不能安全或真实验证，输出 not_verified 及风险接受建议。真实断电、硬件损坏、网络文件系统、多进程竞争、所有 Provider/model、真实 OCR/ASR/外发不能从合成测试推断通过。验证失败时给出重现命令、影响、是否阻塞本地 v1、修复建议，不私自放宽阈值。

运行现有完整 backend、相关 Chromium 和独立 benchmark/lifecycle scripts；生成正式脱敏 evidence，更新状态但不把 benchmark 当 real-pass。验收 Gate I 通过的条件是：阻塞项全部解决或由 10-0 明确接受，支持边界、阈值和未验证列表一致。推荐提交：`test: establish production boundary and capacity evidence`。准确状态：`scoped-gates-pass`，不是全局 production real-pass。
```
