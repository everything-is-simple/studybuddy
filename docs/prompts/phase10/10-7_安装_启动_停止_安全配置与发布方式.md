# 10-7 安装、启动、停止与安全配置发布

```text
请在 H:\\studybuddy 执行 Phase 10-7。先读取共用上下文、README、ARCHITECTURE、启动入口、CLI、配置和现有 scripts。为 local single-process v1 提供可复现的本地发布/运行方式，不引入多 worker、多实例共享 data_root 或云部署。

冻结并实现安全配置 contract：data_root/database/originals topology、端口/host、上传限制、Provider/model/base URL/key 来源、fake/demo 开关、任务并发、备份位置和日志级别。secret 只能 runtime-only/in-memory 或操作系统安全配置来源，不能命令行参数、数据库、日志、artifact 或明文备份。默认配置必须安全：127.0.0.1、本地单进程、provider 未配置或显式 demo、delivery off、无自动 repair/send。

提供 Windows 本地启动/停止、进程锁/重复实例拒绝、健康检查、优雅 shutdown、版本/schema 查询、升级前备份与失败回滚说明。若制作脚本或发布包，只放 scripts/docs/适当发布目录，不提交数据库、originals、secret、生成目录。验证新空目录安装/首次启动、已有 v12 数据启动、配置非法安全失败、停止后重启和端口/锁冲突。

新增 operator/Chromium smoke（若适用）和配置脱敏测试；运行完整 backend 与相关浏览器。不要声称安装器支持所有 Windows 环境，未验证项如实登记。验收 Gate H 通过。推荐提交：`release: add local startup configuration and packaging`。准确状态：`implemented/backend-pass`，实际安装路径通过后可为 `browser-pass`/`scoped-gates-pass`。
```
