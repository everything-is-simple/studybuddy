# 9D-0 立项评审、现状审计与范围冻结

```text
执行 Phase 9D-0：只做立项评审、审计和范围冻结，不实现 S6/S7 业务代码。

使用 ../00_COMMON_CONTEXT.md。Phase 9D 是条件性阶段：本任务必须先产出明确的 go / no-go 立项结论。评审五个立项条件——真实需求、隐私边界、数据保留策略、真实 OCR/ASR/交付组件的可验证证据、运维成本；任一条件不满足时，允许并鼓励结论为“暂不立项”或“仅立项 S6 或 S7 之一”，并说明缺口。不得因为历史版本存在 s6/s7、ocr-tools、report/delivery 就自动判定立项。

完整读取项目权威文档（含 docs/prompts/HISTORICAL_SCENARIO_REVIEW.md 中 S6/S7 线索）并审计实际 backend/app/migrations/runner.py、repository.py、main.py、backup.py、restore_acceptance.py，以及 test_phase8_*、test_phase9a_*、test_phase9b_*、test_phase9c_* 和对应 browser tests。输出每个结论的源码路径、函数名或测试名。

必须盘点：当前 schema/version（确认为 v11）；hash-derived originals 存储与 material delete/restore/purge/re-index lifecycle；material_revisions/chunks/chunk_spans 摄取与 retrieval/citation 管线；provider registry、ai_operations、fake provider、幂等与安全错误；9A/9B/9C 可聚合的计划/进度/练习/错题/薄弱点派生事实；现有导出、backup/verify/restore/restore_acceptance、错误映射与前端结构；当前是否有任何对外网络交付路径。

冻结 S7 课堂采集与 S6 家长报告的用户价值、依赖、隐私与运维风险和 non-goals。明确不把历史项目 S6/S7、真实 OCR/ASR、真实 SMTP/飞书交付、自动定时推送、scheduler/worker、多用户或 Phase 10 能力当现状能力。提出需要在 9D-1 决定的未决问题清单，至少覆盖：采集原件边界与转写置信度语义、转写接入 S2 的方式、报告脱敏白名单/黑名单、对外交付默认关闭与授权/白名单/审计模型、真实组件与 dry-run 的 gate 划分。

只允许修改 docs/prompts/phase9d/ 的审计文档（如需新增，例如 PHASE9D_AUDIT_AND_SCOPE.md）；不改 backend/app、migration、tests、README/STATUS/TODO。验收：立项结论有依据、审计可复核、S6/S7 边界无重叠、隐私与交付风险明确、阻塞项清晰。状态只能写 planned/audit-draft；若结论为暂不立项，则如实记录并停在此处。
```
