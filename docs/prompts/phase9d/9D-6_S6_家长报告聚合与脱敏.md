# 9D-6 S6 家长报告聚合与脱敏

```text
执行 Phase 9D-6：完成 S6 ParentReport 报告聚合与脱敏 backend 闭环，不实现对外交付（9D-7）、API 收口或 UI。

使用 ../00_COMMON_CONTEXT.md、9D-1 契约、9D-3 domain。实现只读报告聚合：按契约的报告类型（日/周/月/考前提醒）从 9A/9B/9C 已有派生事实（计划、进度、练习结果、错题、薄弱点等）生成聚合内容，产出报告对象、预览内容和导出。报告本身不改写任何学习事实。

必须遵守：
- 强制脱敏：只取契约白名单的安全聚合字段；答案 key、用户提交原文、Q&A 原文、原文全文、文件路径、raw provider response、可反推隐私的明细绝不进入报告内容、预览、导出或日志；
- 明确报告是快照还是动态；若快照，保存生成时的聚合事实并保证可重算校验；
- 报告聚合是只读 projection，可重算且不伪造事实，不触碰原件、原文或答案；
- 时间/时区/日期边界按契约由服务端裁定，不信任客户端；
- 空数据、缺失来源、source unavailable 等有稳定且脱敏的表达，不暴露内部原因。

新增 focused backend tests（如 test_phase9d_report.py），覆盖各报告类型聚合、脱敏白名单/黑名单、快照/重算、只读性（不改写学习事实）、时区/日期边界、空/降级数据和导出安全。运行 focused、相关 regression 和完整 backend，用 C:\\miniconda\\py310\\python.exe -m pytest 报告数字。

只允许修改报告聚合相关 repository/domain、导出与对应测试；不实现 9D-7 交付、API 或 Chromium。验收：报告聚合正确、脱敏严格、只读、导出安全通过。状态为 implemented/backend-pass，不代表 9D-7 交付、API/UI、lifecycle/restore gates 或 Phase 9D completed。
```
