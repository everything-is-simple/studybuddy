# 9D-11 完整验收、证据与文档收口

```text
执行 Phase 9D-11：只做 Phase 9D Gate A-L closeout、脱敏证据和文档同步，不偷偷补充新业务。

使用 ../00_COMMON_CONTEXT.md。先确认 9D-0 至 9D-10 每个 gate 的实际证据和状态（含 9D-0 立项结论）。运行项目规定的完整 backend：C:\\miniconda\\py310\\python.exe -m pytest backend/tests/ -q；运行相关 Phase 8/9A/9B/9C regression、browser_phase9d.spec.js 和 UI failure contract。真实 OCR/ASR 与真实对外交付 spec 默认 skip 或显式说明，不把 opt-in 结果扩大为通用 real-pass。

新增 docs/PHASE9D_ACCEPTANCE_EVIDENCE.md，记录范围（含立项结论与裁剪）、schema 版本、migration、S7 采集/转写/接入用户路径、S6 报告聚合/脱敏/交付（dry-run）用户路径、API/domain/browser/lifecycle/backup 证据、实际命令和结果、隐私与脱敏检查、交付默认关闭与授权检查、失败边界和未验证限制。同步 docs/STATUS.md、TODO.md、PHASE_ROADMAP.md、PROJECT_PROGRESS_REPORT.md、INDEX.md、ARCHITECTURE/ai-learning-architecture（仅在事实需要时）。不要保留与唯一状态源冲突的旧数字。

只有所有 Gate A-L 实际通过，才允许使用限定声明：Phase 9D 已在 deterministic fake/loopback 组件、单进程 SQLite、本地 Chromium、backup/restore 与本地 dry-run 交付的明确范围内完成。必须同时写明不包含：真实 OCR/ASR provider 通用验证、真实对外交付（SMTP/飞书生产端点）、自动 scheduler/worker/定时推送、多用户/云同步、系统级 screen reader、极端内容、长时稳定性和全局 production real-pass。任何 focused/backend/browser/restore 局部通过只能写对应局部状态。若 9D-0 结论为部分立项或暂不立项，如实记录实际完成范围，不夸大。

本任务允许修改 docs/ 和新增 evidence/test artifact（不得提交数据库、上传原件、secret、路径含私人数据或测试输出）；完成后报告每个 gate、命令、结果、准确完成措辞和遗留限制。
```
