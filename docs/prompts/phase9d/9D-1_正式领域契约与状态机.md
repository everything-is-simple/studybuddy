# 9D-1 正式领域契约与状态机

```text
执行 Phase 9D-1：只冻结正式领域契约与状态机，不实现 schema/API/UI。

使用 ../00_COMMON_CONTEXT.md 和 9D-0 立项结论。仅在 9D-0 判定立项（或部分立项）的范围内冻结契约；被判定暂不立项的子系统不在本任务范围。

冻结 S7 课堂采集契约：
- capture session 的 owner/scope/status 与生命周期；采集原件（音频/图片）类型、大小边界与纳入 hash-derived originals/material 的关系；
- OCR/ASR 转写 operation 的输入/输出、置信度语义、uncertain/low-confidence 标注、失败/超时/乱码处理；真实 provider 与 deterministic fake/loopback 的边界与 gate；raw request/response 不持久化边界；
- 转写接入 S2 的方式（新 material 还是新 revision）、citation/source 校验、draft → confirm、用户编辑保护。

冻结 S6 家长报告契约：
- report 对象 owner/scope、类型（日/周/月/考前提醒）、聚合数据源与派生规则、快照还是动态、报告状态机；
- 脱敏契约：明确可进入报告的安全聚合字段白名单，以及绝对禁止的黑名单（答案 key、提交原文、Q&A 原文、原文全文、文件路径、可反推隐私明细）；
- 对外交付契约：渠道抽象、配置与 secret 存放（不入库/不入日志/不入 backup 明文）、显式开关默认关闭、显式授权、收件白名单、dry-run vs 真实发送、审计记录字段、重试与去重、禁止自动定时推送；真实发送是否排除。

同时冻结：哪些事实 append-only、哪些是可重算 projection；SQLite constraint 与 domain transaction 的分工；错误码；API resource 命名；导出格式；source lifecycle 与 backup/restore non-repair 边界（不重新 OCR/ASR、不重算外发、不在 restore/startup 触发交付）。

只允许修改 docs/prompts/phase9d/ 契约文档（如 PHASE9D_DOMAIN_CONTRACT.md）；不改 backend/app、migration、tests。验收：契约可实现、状态机闭合、隐私与交付边界无歧义、与 9A/9B/9C/originals 契约不冲突。状态只能写 planned/contract-frozen，不代表实现完成。
```
