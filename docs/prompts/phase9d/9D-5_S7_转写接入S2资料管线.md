# 9D-5 S7 转写接入 S2 资料管线

```text
执行 Phase 9D-5：在 9D-4 采集/转写基础上完成转写接入 S2 资料管线，不实现 S6 报告/交付、API 收口或 UI。

使用 ../00_COMMON_CONTEXT.md 和 9D-1 契约。按契约把 S7 转写文本接入既有 materials/extractions/material_revisions/chunks/chunk_spans 管线，不新建平行正文体系。明确接入方式（新 material 还是新 revision），并完整复用既有摄取、retrieval 和 citation 验证。

必须遵守：
- 转写进入资料管线时保留 source revision 与可验证 citation，可回链到 capture session 与置信度；
- 接入产物先是 draft/建议，不静默覆盖用户已有编辑、confirmed artifact、note/module、attempt 或 review；用户显式 confirm/reject/archive 才改变状态；
- 低置信/uncertain 内容在接入后仍可被用户识别与核对，AI 不作最终裁决；
- source deleted/purged/stale/unavailable 时不伪造正文、材料名、路径、quote 或可用 citation；历史转写与接入记录保留，source 状态只降级；
- indexing/retrieval 复用既有确定性行为，不引入新的后台扫描或 worker。

新增 focused backend tests（如 test_phase9d_capture_ingest.py），覆盖接入为 material/revision、citation/source 校验、draft → confirm/reject、用户编辑保护、检索可命中转写内容、uncertain 保留、source lifecycle 降级和 rollback。运行 focused、相关 Phase 9B/retrieval regression 和完整 backend，用 C:\\miniconda\\py310\\python.exe -m pytest 报告数字。

只允许修改接入相关 repository/domain 与对应测试；不实现 S6 报告/交付、API 或 Chromium。验收：转写安全接入、citation 可追溯、编辑保护、检索可用、source lifecycle 正确通过。状态为 implemented/backend-pass，不代表 S6、API/UI、lifecycle/restore gates 或 Phase 9D completed。
```
