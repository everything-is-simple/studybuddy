# 9B-4：S2 资料笔记与知识模块工作流

> 先使用 `00_COMMON_CONTEXT.md`、9B-1 契约和 9B-3 domain 基础。本任务先完成 S2 domain/workflow，不实现完整 S1 UI。

```text
执行 Phase 9B-4：实现 S2“资料笔记与知识模块”的正式工作流，不复制历史 KnowledgeModule 实现，不扩展到 S3/S4/S5。

实现最小可验收闭环：从 active material 或已有 revision/chunk scope 创建用户 note；编辑 note/note block；将 note 与一个或多个已验证的 source revision/chunk/span/citation 关联；创建和组织 knowledge module；把 note/module 标记为 draft、user-edited、confirmed、archived 或 contract 规定的状态；显式使用 deterministic fake provider 生成 citation-safe note/module draft；服务器重新验证 retrieval/context/citation 后才保存；用户可编辑、确认、拒绝、归档，任何重试不得覆盖用户编辑或 confirmed 状态。

明确区分：原始材料正文是 source of truth；note/module 是用户或 AI 派生 artifact；citation 是可验证 provenance，不是无来源正文的替代品。source delete/restore/purge/new revision/re-index 后显式刷新状态，不自动 repair、重新生成或伪造材料名称/正文。

覆盖：空检索、未索引、provider_not_configured、timeout/unavailable、malformed structured output、伪造 citation、重复请求/idempotency、失败 rollback、source unavailable、编辑保护和隐私响应。新增 S2 focused backend tests，并输出可供 API/UI 复用的稳定 domain contract。

本任务不实现 S1 节奏 API/UI、提醒/调度、真实 Provider generation、人工审核、OCR/ASR、S3/S4/S5。

验收：S2 domain/workflow focused tests 通过，状态使用 `implemented/backend-pass` 或更准确的局部措辞；不得把 S2 完成扩大解释为 Phase 9B completed。
```