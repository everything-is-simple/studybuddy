# 9B-5：S1 学习节奏工作流

> 先使用 `00_COMMON_CONTEXT.md`、9B-1 契约和 9B-3 domain 基础。本任务不实现后台 scheduler 或完整 UI。

```text
执行 Phase 9B-5：实现 S1“学习节奏”的显式、同步、可重算工作流。

复用 Phase 9A study plan、study plan item、dependency、append-only progress，不另造第二套计划事实源。按冻结契约实现：节奏周期和 timezone；每日/每周或契约允许的周期目标；学习时段或可用时间；item 的工作量估计、分配、移动、跳过、完成和 reopen；时间线/负载/逾期或未安排 summary；用户手动调整和保护；非法日期、timezone、负数/超限工作量、跨 plan dependency、重复操作和冲突处理。

所有计算必须确定性、可测试，不读取宿主机隐式 timezone，不启动定时任务，不发送提醒，不自动执行 item，不自动重排或覆盖用户安排。progress event 仍 append-only，summary 可由事实数据重算；刷新和恢复不能凭空生成 progress 或计划项。

覆盖 domain transaction、rollback、SQLite lock、失败后 retry、空计划、过载节奏、日期边界、DST/timezone（如契约纳入）、completed item 保护、source unavailable warning 与 plan/item 状态一致性。新增 S1 focused backend tests。

本任务不实现 S2 note/module API/UI、scheduler/worker、提醒推送、真实 Provider plan generation、S3/S4/S5。

验收：S1 focused tests 通过，状态使用 `implemented/backend-pass`；不得宣称 S1 browser-pass 或 Phase 9B completed。
```