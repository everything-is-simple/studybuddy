window.sbApi={async json(path,options={}){const response=await fetch(path,{...options,headers:{Accept:'application/json',...(options.headers||{})}});let body=null;try{body=await response.json()}catch(_){ }if(!response.ok){const error=new Error(body&&body.detail&&typeof body.detail==='string'?body.detail:'request_failed');error.status=response.status;throw error}return body},async upload(path,files,options={}){const form=new FormData();if(Array.isArray(files)){files.forEach(f=>form.append('files',f))}else{form.append('file',files)}const response=await fetch(path,{method:'POST',...options,body:form,headers:{Accept:'application/json',...(options.headers||{})}});let body=null;try{body=await response.json()}catch(_){ }if(!response.ok){const error=new Error(body&&body.detail&&typeof body.detail==='string'?body.detail:'upload_failed');error.status=response.status;throw error}return body},idempotencyKey(){return'ui-'+Date.now()+'-'+Math.random().toString(36).slice(2,11)},safeError(error){const known={material_persist_failed:'材料保存失败，请重试',database_unavailable:'数据暂时不可用，请稍后重试',service_not_ready:'系统正在启动，请稍后重试',not_found:'内容不存在或已不可用',request_failed:'请求失败，请重试',upload_failed:'上传失败，请重试',invalid_filename:'文件名无效',qa_invalid_materials:'材料选择无效',retrieval_invalid_mode:'检索模式无效',qa_invalid_idempotency_key:'请求标识无效'};return known[error.message]||'请求失败，请重试'}};window.sbSubmit={_pending:new Map(),async once(key,fn){if(this._pending.has(key))return this._pending.get(key);const promise=fn().finally(()=>this._pending.delete(key));this._pending.set(key,promise);return promise}};
