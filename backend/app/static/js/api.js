window.sbApi={async json(path,options={}){const response=await fetch(path,{...options,headers:{Accept:'application/json',...(options.headers||{})}});let body=null;try{body=await response.json()}catch(_){ }if(!response.ok){const error=new Error(body&&body.detail&&typeof body.detail==='string'?body.detail:'request_failed');error.status=response.status;throw error}return body},safeError(error){const known={material_persist_failed:'材料保存失败，请重试',database_unavailable:'数据暂时不可用，请稍后重试',service_not_ready:'系统正在启动，请稍后重试',not_found:'内容不存在或已不可用',request_failed:'请求失败，请重试'};return known[error.message]||'请求失败，请重试'}};
